# Browser-only photo and mock generator
Own src/platform/photo.ts, src/generator/index.ts, tests/photo-routing.test.ts, docs/photo-decisions.md only. Root implements UI/render; do not change package/config/otherfiles, no subagents/commits. Existing @mediapipe/tasks-vision and heic2any installed. Local public/wasm and public/models/face-detector.tflite already bundled; never external inference/network image APIs. No photo upload.
Implement decodePhoto(file:File):Promise<PreparedPhoto> with PreparedPhoto {image:HTMLImageElement,objectUrl:string,metadata:{mimeType:string,byteSize:number,width:number,height:number}}. Input JPEG PNG HEIC <=10_000_000 bytes only; HEIC converted locally via dynamic import heic2any; metadata retains original mime/size. Browser decode auto EXIF orientation. Fail in Chinese, release URL via exported releasePhoto. Pixel limits for processing scale to max2048 but shortedge warning based original dims. Avoid duplicate decode giant memory or bound explicit pixel safety allocation only, not new photo rejection constraints.
detectFaces(prepared):Promise<DetectedFace[]> loads MediaPipe FaceDetector from local /wasm and /models/face-detector.tflite, CPU, IMAGE, minDetectionConfidence recommendation0.4. Perform only after user explicit face consent via UI. Returns normalized boxes {x,y,w,h}, angle:'F'|'L'|'R'|'UNCERTAIN', confidence:number. Keypoints coarse yaw using nose/eye center; uncertainty F by existing route later. Multi face requires UI chooseindex. Never manual fake detection/no face bypass. Pure helper classifyFaceAngle using relative keypoints unit tests including uncertainty.
Generator interface required generate(input,templateVersion,poseId,mirror,drawResult)->Promise<Artwork>. Define GeneratorInput {photo:PreparedPhoto,face:DetectedFace}; Artwork {canvas:HTMLCanvasElement,width:number,height:number,kind:'mock',debug:{poseId,mirror}}. Async network lifecycle handled backend not here. MockGenerator creates 1380x1485 opaque canvas pure dark blue background + human basketball jersey silhouette with visiblearms/ball, head cropped original photo at chosen face; include debug poseId/mirror text drawn in artwork. Requested art intentionally mock, no real generation. Avoid cropping useridentity away; use oval head crop with margins or rectangularphoto cutout; mirror onlybody/pose not photographface (record decision). RoutesG/F/C and L/R should visibly changepose direction simple silhouette. No names/jerseynumbers/cardId frominput everdrawn by generator; rootprogramlayout handles those. No generated AI API. RealGenerator class same interface throws explicit NOT_CONFIGURED error (empty contract implementation). Tests pure detection/routing logic only; integration verified rootbrowser. Reports docs/briefs/photo-report.md concise.
