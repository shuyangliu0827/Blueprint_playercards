# Backend batch 3
Work only in src/server/preview-service.ts, src/core/mock-timing.ts, src/app/api/preview/route.ts, config/generation_contract.json, tests/preview-service.test.ts, tests/mock-timing.test.ts and docs/backend-decisions.md. Do not edit other files/package or commit. No subagents. Existing batch-one core/config authoritative, read them. Build tested memory-only async mock jobs and Next.js App Router single API POST /api/preview. Server must keep HMAC secret/probabilities private. No image uploads. Strict request whitelist and max body 20 KB. Existing loadConfiguration/createDrawService validate before startup. Secret env DRAW_HMAC_SECRET/IP_HASH_SALT min32byte. Cookie bp_anon server signed (anonId+HMAC); session action creates or reads only signed cookie; expose anonId for localStorage mirror. No currentstate in draw; draw(requestId) existing parsed firstflag/version.
API bodies/actions and output contracts:
{action:'session',ref?:string} -> {anonId,configVersion,remaining,successfulCount,refToken}; refToken signed ref linking current anon ID. Validated ref opened via session -> trusted shared_link_opened once per visitor, exclude self, ignore forged ref. Must be service observable, client cannot submit trusted events. All API responses no-store. Cookie httpOnly,sameSiteLax,secure production.
{action:'submit',requestId,input:CardInput,mode:'normal'|'fast',scenario:'random'|'success'|'retry'|'fail',reason:'initial'|'new'} -> JobView
{action:'poll',requestId} -> JobView
{action:'complete',requestId} -> JobView (idempotent after artwork rendered, counts successful issue only once)
{action:'restore',requestId} -> JobView (same draw, reserve if quota available, same task; recovery succeeds after mock delay; no extra repeat-generation)
{action:'render-failed',requestId} -> JobView (release reservation retain draw)
{action:'events',events:unknown[]} -> {accepted:number}; sanitize actorAnonId to own cookie, acceptClientEvent strict whitelist, dedup eventId. Never rawIP or image/name fields in events.
{action:'metrics'} -> {metrics,events,volatile:true} ONLY own actor events and referral events where creator is self; no leak others' events.
JobView shape: {requestId,status:'pending'|'retrying'|'ready'|'failed'|'complete',draw:{tier,cardId,configVersion},poseId:string,mirror:boolean,attempt:number,remaining:number,successfulCount:number,elapsedMs:number,retryAtMs?:number,error?:string}.
Implement PreviewService configurable clock and random for deterministic tests; route singleton on globalThis. Memory loss accepted and documented. On unknown request poll returns meaningful TASK_EXPIRED and UI can resubmit same requestId (same draw). Do not use timers/backgroundwork, compute status by elapsed time when polled. Reserve max3 across pending jobs; success only on complete; invalid/failure/retry not counted. failure15% with10%retrythen success,5%terminal (first attempt fail then same-result second attempt fail), one retry, refund. Explicit scenarios internalreview allowed. Default latency continuouspiecewise quantiles with P50=12000,P95=40000, fast500; failureclass separate PRNG. Store rawinput only ephemeral if needed, no image allowed inside photo metadata. store successful outcome only necessarymetadata, no names in analytics. Record generation events server through existing acceptClientEvent (dictionary allows). Do not doublecount client generation events—UI sends only export/view interactions.
IP saltedHMAC aggregation only memory, no rawIP. >=5 distinctanon per rolling3600000ms main; newanon within600000ms of3successes sameIP aux. emit acceptIpPollutionEvent, no blocks, no filteringusersmetrics. Route trusted x-forwarded-for on Vercel may take first; document platform adaptertrust. Prune expiredmemory withbounded limits (avoid users' active taskloss unnecessarily).
Unit tests concurrencyreservation,firstsuccess, failuresretryrefund,recovery,requestreplay,strictinput/noimages, cookies/refsignverify/dedupself exclusion, event spoofingrejection, rawIPabsence, IP thresholdexact, memoryexpiry. Pure mock timing test quantiles. RealGenerator NOT part yourtask.
Write report docs/briefs/backend-report.md with API, tests,concerns.
